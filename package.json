{
  "name": "calculator",
  "version": "1.0.0",
  "description": "A simple calculator with string addition",
  "main": "calculator.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
